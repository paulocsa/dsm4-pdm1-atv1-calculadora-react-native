import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  container: {
    marginTop: "50%", // A margem superior será 50% da altura da tela
    flexDirection: "row",
    flexWrap: "wrap",
    justifyContent: "center",
    padding: 10,
    gap: 10,
  },
});

export default styles;
